<aside class="main-sidebar elevation-4 sidebar-dark-info">
    <a href="javascript:;" class="brand-link text-center">
      <img src="/dist/img/kite-logo.png" class="brand-image img-circle elevation-3">
      <span class="brand-text font-weight-light">
        KITE-Admin-Board
      </span>
    </a>
    <div class="sidebar">
      <div class="user-panel mt-3 pb-3 mb-3 d-flex">
        <div class="image">
          <img src="/dist/img/user/user-image.png" class="img-circle elevation-2" alt="User Image">
        </div>
        <div class="info">
          <a href="javascript:;" class="d-block">Bruce Wayne</a>
        </div>
      </div>
      <nav class="mt-2">
        <ul class="nav nav-pills nav-sidebar flex-column nav-compact nav-child-indent" data-widget="treeview" role="menu" data-accordion="false">
          <% var myObject = [ 
                { heading: "MAIN NAVIGATION" }, 
                { href: "/admin/home" , route: "home" , icon: "fas fa-tachometer-alt" ,title: "Dashboard" }, 
                {heading: 'USER MANAGEMENTS' }, 
                { href: "/admin/user" , route: "user" , icon: "fa fa-user" , title: "Users" },
                {heading: 'CATEGORY' }, 
                { href: "/admin/category" , route: "category" , icon: "fa fa-user" , title: "Category" },
                ]; %>
  
          <% myObject.forEach((item)=> { %>
          <% if (item.heading) { %>
          <li class="nav-header">
            <%= item.heading %>
          </li>
          <% } else if (item.submenu && item.submenu.length> 0) { %>
          <% var menuActive='' ; %>
          <% item.submenu.forEach((sMenu)=> { %>
          <% if (route===sMenu.route) { %>
          <% menuActive='active' ; %>
          <% } %>
          <% }); %>
          <li class="nav-item <%= menuActive ? 'menu-open' : ''%>">
            <a href="javascript:void(0)" class="nav-link <%= menuActive %>">
              <i class="nav-icon <%= item.icon %>" style="vertical-align: text-bottom;"></i>
              <p>
                <%= item.title %><i class="right fas fa-angle-left"></i>
              </p>
            </a>
            <ul class="nav nav-treeview">
              <% item.submenu.forEach((subItem)=> { %>
              <li class="nav-item">
                <a href="<%= subItem.href %>" class="nav-link <%= route === subItem.route ? 'active' : '' %>">
                  <i class="nav-icon <%= subItem.icon ?? 'far fa-circle' %>" style="vertical-align: text-bottom;"></i>
                  <p>
                    <%= subItem.title %>
                  </p>
                </a>
              </li>
              <% }); %>
            </ul>
          </li>
          <% } else { %>
          <li class="nav-item">
            <a href="<%= item.href %>" class="nav-link <%= route === item.route ? 'active' : '' %>">
              <i class="nav-icon <%= item.icon ?? '' %>" style="vertical-align: text-bottom;"></i>
              <p>
                <%= item.title ?? '' %>
              </p>
            </a>
          </li>
          <% } %>
          <% }); %>
        </ul>
      </nav>
    </div>
  </aside>